"""Archivo temporal neutralizado (no usar). Se mantiene para evitar conflictos de borrado en caliente."""

__all__: list[str] = []
                    rx.cond(
                        CardiaState.error.is_not_none(),
                        rx.text(lambda: CardiaState.error, color="red"),
                    ),
                    rx.textarea(
                        value=CardiaState.output_json,
                        width="100%",
                        height="460px",
                        is_read_only=True,
                    ),
                    width="50%",
                    padding="1rem",
                ),
                width="100%",
            ),
            rx.link(
                "Ver HTML Legacy",
                href="/legacy/cardiaia.html",
                _hover={"text_decoration": "underline"},
            ),
            width="100%",
            padding="1rem",
        ),
        width="100%",
    )


def get_page():
    return cardiaia_page()

__all__ = ["get_page", "cardiaia_page", "CardiaState"]
